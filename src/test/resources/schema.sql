CREATE TABLE Person (
                        id INT AUTO_INCREMENT PRIMARY KEY,
                        name VARCHAR(255),
                        street VARCHAR(255),
                        city VARCHAR(255),
                        country VARCHAR(255),
                        phoneNumber VARCHAR(255),
                        email VARCHAR(255),
                        flag BOOLEAN
);